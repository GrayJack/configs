return {
    conditional = require( "dynamite.layout.conditional" );
    ratio       = require( "dynamite.layout.ratio"       );
    stack       = require( "dynamite.layout.stack"       );
    tabbed      = require( "dynamite.layout.tabbed"      );
}
